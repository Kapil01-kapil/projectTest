class Place {
  constructor(id, FullName, gender, address, email, password) {
    this.id = id;
    this.FullName = FullName;
    this.gender = gender;
    this.address = address;

    this.email = email;
    this.password = password;
  }
}

export default Place;
